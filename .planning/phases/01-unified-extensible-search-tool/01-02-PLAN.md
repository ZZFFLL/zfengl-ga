---
phase: "01"
plan: "02"
title: "Expose one GA search tool and verify smoke path"
type: "implementation"
wave: 2
depends_on:
  - "01-01"
files_modified:
  - "ga.py"
  - "assets/tools_schema.json"
  - "assets/tools_schema_cn.json"
  - "tests/test_search_tool_integration.py"
  - "tests/test_searchserver_smoke.py"
autonomous: true
requirements_addressed:
  - "SRCH-01"
  - "SRCH-02"
  - "SRCH-03"
  - "PROV-01"
  - "PROV-02"
  - "PROV-03"
  - "PROV-04"
  - "INTG-01"
  - "INTG-02"
  - "INTG-03"
must_haves:
  truths:
    - "D-02: GA discovers exactly one model-visible search service tool through the existing GA tool exposure path."
    - "D-03: Provider count must not leak into the model-visible tool list."
    - "D-05: The model-facing tool input is only the search keyword/query."
    - "D-06: Success payloads are structured and include provider, query, and result items."
    - "D-11: Provider failure falls back to the next available service."
    - "D-12: If all providers fail, return that search cannot be performed."
    - "D-13: All-failed responses include every provider's error reason."
    - "D-14: Verification includes real provider smoke coverage."
    - "D-15: Tests cover schema presence, discovery, invocation, fallback, and errors."
  artifacts:
    - path: "ga.py"
      provides: "One GenericAgentHandler.do_web_search wrapper"
    - path: "assets/tools_schema.json"
      provides: "English schema for the single model-visible search tool"
    - path: "assets/tools_schema_cn.json"
      provides: "Chinese schema for the single model-visible search tool"
    - path: "tests/test_search_tool_integration.py"
      provides: "Schema and handler integration coverage"
    - path: "tests/test_searchserver_smoke.py"
      provides: "Real Tavily smoke coverage gated by local config"
  key_links:
    - "ga.py delegates provider behavior to tools/searchserver/"
    - "assets/tools_schema*.json contain exactly one search tool entry"
---

<objective>
Expose exactly one model-visible GA search tool through the existing schema and `GenericAgentHandler.do_*` dispatch path, delegating all provider behavior to `tools/searchserver/`. Add deterministic integration tests and a real Tavily smoke test path that runs only when local `mykey.py` config provides usable credentials.
</objective>

<must_haves>
<truth id="D-02">GA discovers exactly one model-visible search service tool through the existing GA tool exposure path.</truth>
<truth id="D-03">Provider count must not leak into the model-visible tool list.</truth>
<truth id="D-05">The model-facing tool input is only the search keyword/query.</truth>
<truth id="D-06">Success payloads are structured and include provider, query, and result items.</truth>
<truth id="D-11">Provider failure falls back to the next available service.</truth>
<truth id="D-12">If all providers fail, return that search cannot be performed.</truth>
<truth id="D-13">All-failed responses include every provider's error reason.</truth>
<truth id="D-14">Verification includes real provider smoke coverage.</truth>
<truth id="D-15">Tests cover schema presence, discovery, invocation, fallback, and errors.</truth>
</must_haves>

## Decision Coverage

- D-02: GA discovers exactly one model-visible search service tool through the existing GA tool exposure path.
- D-03: Provider count must not leak into the model-visible tool list.
- D-05: The model-facing tool input is only the search keyword/query.
- D-06: Success payloads are structured and include provider, query, and result items.
- D-11: Provider failure falls back to the next available service.
- D-12: If all providers fail, return that search cannot be performed.
- D-13: All-failed responses include every provider's error reason.
- D-14: Verification includes real provider smoke coverage.
- D-15: Tests cover schema presence, discovery, invocation, fallback, and errors.

<tasks>
<task id="1" type="inspect">
  <files>
    <file>ga.py</file>
    <file>assets/tools_schema.json</file>
    <file>assets/tools_schema_cn.json</file>
    <file>tests/test_agent_loop_events.py</file>
  </files>
  <action>Inspect current tool naming, handler return style, and schema shape. Choose a single model-visible tool name, recommended `web_search`, to complement `web_scan` and `web_execute_js` without changing their semantics.</action>
  <verify>Confirm there is exactly one new tool schema entry planned and exactly one matching `GenericAgentHandler.do_<tool>()` method.</verify>
  <acceptance_criteria>
    <criterion>Tool name is stable and does not imply provider-specific behavior.</criterion>
    <criterion>Browser tool semantics remain untouched.</criterion>
  </acceptance_criteria>
</task>

<task id="2" type="implement">
  <files>
    <file>ga.py</file>
  </files>
  <action>Add a thin `GenericAgentHandler.do_web_search()` wrapper that extracts `keyword` or `query`, calls the `tools.searchserver` provider-layer entry point, yields concise `[Info]` status, and returns `StepOutcome(result, next_prompt=...)` consistent with adjacent tools.</action>
  <verify>Run a targeted Python snippet or unit test that calls `GenericAgentHandler.do_web_search()` with a fake provider-layer result.</verify>
  <acceptance_criteria>
    <criterion>The handler accepts only the search keyword/query at the model-facing boundary.</criterion>
    <criterion>The handler does not contain provider-specific API logic.</criterion>
    <criterion>The handler returns structured success/error payloads without raising provider exceptions through the loop.</criterion>
  </acceptance_criteria>
</task>

<task id="3" type="implement">
  <files>
    <file>assets/tools_schema.json</file>
    <file>assets/tools_schema_cn.json</file>
  </files>
  <action>Add one `web_search` function schema to both English and Chinese schema files. The only required semantic input is the search keyword/query. Keep the description clear that this is for external search when browser page inspection is insufficient.</action>
  <verify>Load both JSON files with `json.load()` and assert exactly one new search tool entry exists in each.</verify>
  <acceptance_criteria>
    <criterion>Both schema files stay valid JSON.</criterion>
    <criterion>The schema does not expose provider-specific options or provider-specific tools.</criterion>
  </acceptance_criteria>
</task>

<task id="4" type="test">
  <files>
    <file>tests/test_search_tool_integration.py</file>
  </files>
  <action>Add tests covering schema presence, handler dispatch through `BaseHandler.dispatch()`, success payload forwarding, and all-failed payload forwarding. Use fakes/monkeypatches for the provider layer.</action>
  <verify>Run `python -m pytest tests/test_search_tool_integration.py`.</verify>
  <acceptance_criteria>
    <criterion>Tests fail if schema name and handler name drift apart.</criterion>
    <criterion>Tests prove only one model-visible search tool is added.</criterion>
    <criterion>Tests prove provider errors are returned as tool data.</criterion>
  </acceptance_criteria>
</task>

<task id="5" type="test">
  <files>
    <file>tests/test_searchserver_smoke.py</file>
  </files>
  <action>Add a real Tavily smoke test that is skipped unless local config exposes Tavily key(s) and endpoint URL. The smoke should perform a small harmless query and assert a structured successful result. It must not print secrets.</action>
  <verify>Run `python -m pytest tests/test_searchserver_smoke.py` once without credentials to confirm skip behavior, and once with local Tavily config when available to confirm real provider behavior.</verify>
  <acceptance_criteria>
    <criterion>Smoke test is safe to run in normal environments without credentials.</criterion>
    <criterion>When credentials exist, the smoke exercises the real provider path.</criterion>
    <criterion>Failure output does not leak keys.</criterion>
  </acceptance_criteria>
</task>

<task id="6" type="verify">
  <files>
    <file>ga.py</file>
    <file>assets/tools_schema.json</file>
    <file>assets/tools_schema_cn.json</file>
    <file>tools/searchserver/</file>
    <file>tests/test_searchserver.py</file>
    <file>tests/test_search_tool_integration.py</file>
    <file>tests/test_searchserver_smoke.py</file>
  </files>
  <action>Run the focused verification set for the phase and inspect the diff for accidental provider-tool multiplication or secret leakage.</action>
  <verify>
    <command>python -m py_compile ga.py tools/searchserver/*.py tools/searchserver/providers/*.py mykey_template.py mykey_template_en.py</command>
    <command>python -m pytest tests/test_searchserver.py tests/test_search_tool_integration.py</command>
    <command>python -m pytest tests/test_searchserver_smoke.py</command>
    <command>git diff --check</command>
  </verify>
  <acceptance_criteria>
    <criterion>Focused deterministic tests pass.</criterion>
    <criterion>Smoke test either passes with real config or skips cleanly without credentials.</criterion>
    <criterion>`git diff --check` reports no whitespace errors.</criterion>
  </acceptance_criteria>
</task>
</tasks>

<verification>
<step>Run `python -m py_compile ga.py tools/searchserver/*.py tools/searchserver/providers/*.py mykey_template.py mykey_template_en.py`.</step>
<step>Run `python -m pytest tests/test_searchserver.py tests/test_search_tool_integration.py`.</step>
<step>Run `python -m pytest tests/test_searchserver_smoke.py` and record whether it passed with real Tavily config or skipped due to missing local credentials.</step>
<step>Run `git diff --check`.</step>
</verification>

<success_criteria>
- Exactly one new model-visible search tool exists in both schema files.
- `GenericAgentHandler.do_web_search()` delegates provider behavior to `tools/searchserver/`.
- `web_scan` and `web_execute_js` are not semantically changed.
- All v1 requirements and discuss decisions are covered by plans and tests.
- Real provider smoke verification is available and safe when credentials are absent.
</success_criteria>
